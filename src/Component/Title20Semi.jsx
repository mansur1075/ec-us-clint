import React from 'react'

const Title20Semi = ({ children, className }) => {
    return (
        <div className={`font-Inter font-medium text-[20px] leading-[20px] tracking-[3%] ${className}`}>{children}</div>
    )
}

export default Title20Semi